# -- Override the name of the chart
nameOverride: ""
# -- Override the full name of the release
fullnameOverride: ""

# -- Name of the Kubernetes cluster monitored. Can be configured also with `global.cluster`.
# Note it will be set as an external label in prometheus configuration, it will have precedence over `config.common.external_labels.cluster_name`
# and `customAttributes.cluster_name``.
cluster: ""
# -- This set this license key to use. Can be configured also with `global.licenseKey`
licenseKey: ""
# -- In case you don't want to have the license key in you values, this allows you to point to a user created secret to get the key from there. Can be configured also with `global.customSecretName`
customSecretName: ""
# -- In case you don't want to have the license key in you values, this allows you to point to which secret key is the license key located. Can be configured also with `global.customSecretLicenseKey`
customSecretLicenseKey: ""

# -- Adds extra attributes to prometheus external labels. Can be configured also with `global.customAttributes`. Please note, values defined
# in `common.config.externar_labels` will have precedence over `customAttributes`.
customAttributes: {}

# Images used by the chart for prometheus and New Relic configurator.
# @default See `values.yaml`
images:
  # -- The secrets that are needed to pull images from a custom registry.
  pullSecrets: []

  # -- Image for New Relic configurator.
  # @default -- See `values.yaml`
  configurator:
    registry: ""
    repository: newrelic/newrelic-prometheus-configurator
    pullPolicy: IfNotPresent
    # @default It defaults to `annotation.configuratorVersion` in `Chart.yaml`.
    tag: ""
  # -- Image for prometheus which is executed in agent mode.
  # @default -- See `values.yaml`
  prometheus:
    registry: ""
    repository: quay.io/prometheus/prometheus
    pullPolicy: IfNotPresent
    # @default It defaults to `appVersion` in `Chart.yaml`.
    tag: ""

# -- Volumes to mount in the containers
extraVolumes: []
# -- Defines where to mount volumes specified with `extraVolumes`
extraVolumeMounts: []

# -- Settings controlling ServiceAccount creation.
# @default -- See `values.yaml`
serviceAccount:
  # -- Whether the chart should automatically create the ServiceAccount objects required to run.
  create: true
  annotations: {}
  # If not set and create is true, a name is generated using the full name template
  name: ""

# -- Additional labels for chart objects. Can be configured also with `global.labels`
labels: {}
# -- Annotations to be added to all pods created by the integration.
podAnnotations: {}
# -- Additional labels for chart pods. Can be configured also with `global.podLabels`
podLabels: {}

# -- Resource limits to be added to all pods created by the integration.
# @default -- `{}`
resources:
  prometheus: {}

# -- Sets pod's priorityClassName. Can be configured also with `global.priorityClassName`
priorityClassName: ""
# -- (bool) Sets pod's hostNetwork. Can be configured also with `global.hostNetwork`
# @default -- `false`
hostNetwork:
# -- Sets security context (at pod level). Can be configured also with `global.podSecurityContext`
podSecurityContext: {}
# -- Sets security context (at container level). Can be configured also with `global.containerSecurityContext`
containerSecurityContext: {}

# -- Sets pod's dnsConfig. Can be configured also with `global.dnsConfig`
dnsConfig: {}

# Settings controlling RBAC objects creation.
rbac:
  # -- Whether the chart should automatically create the RBAC objects required to run.
  create: true
  # -- Whether the chart should create Pod Security Policy objects.
  pspEnabled: false

# -- Sets pod/node affinities set almost globally. (See [Affinities and tolerations](README.md#affinities-and-tolerations))
affinity: {}
# -- Sets pod's node selector almost globally. (See [Affinities and tolerations](README.md#affinities-and-tolerations))
nodeSelector: {}
# -- Sets pod's tolerations to node taints almost globally. (See [Affinities and tolerations](README.md#affinities-and-tolerations))
tolerations: []

# -- (bool) Send the metrics to the staging backend. Requires a valid staging license key. Can be configured also with `global.nrStaging`
# @default -- `false`
nrStaging:

# -- (bool) Reduces the number of metrics sent in order to reduce costs. It can be configured also with `global.lowDataMode`.
# Specifically, it makes Prometheus stop reporting some Kubernetes cluster-specific metrics, you can see details in `static/lowdatamodedefaults.yaml`.
# @default -- false
lowDataMode:

# -- It holds the configuration for metric type override. If enabled, a series of metric relabel configs will be added to
# `config.newrelic_remote_write.extra_write_relabel_configs`, you can check the whole list in `static/metrictyperelabeldefaults.yaml`
metric_type_override:
  enabled: true

# -- Set up Prometheus replicas to allow horizontal scalability.
# @default -- See `values.yaml`
sharding:
  # -- Sets the number of Prometheus instances running on sharding mode.
  # @default -- `1`
  # total_shards_count:

# -- (bool) Sets the debug log to Prometheus and prometheus-configurator or all integrations if it is set globally. Can be configured also with `global.verboseLog`
# @default -- `false`
verboseLog:

# -- It holds the New Relic Prometheus configuration. Here you can easily set up Prometheus to get set metrics, discover
# ponds and endpoints Kubernetes and send metrics to New Relic using remote-write.
# @default -- See `values.yaml`
config:
  # -- Include global configuration for Prometheus agent.
  # @default -- See `values.yaml`
  common:
    # -- The labels to add to any timeseries that this Prometheus instance scrapes.
    # @default -- `{}`
    # external_labels:
    #   label_key_example: foo-bar
    # -- How frequently to scrape targets by default, unless a different value is specified on the job.
    scrape_interval: 30s
    # -- The default timeout when scraping targets.
    # @default -- `10s`
    # scrape_timeout:

  # -- (object) Newrelic remote-write configuration settings.
  # @default -- See `values.yaml`
  newrelic_remote_write:
    # # -- Includes additional [relabel configs](https://prometheus.io/docs/prometheus/latest/configuration/configuration/#relabel_config)
    # # for the New Relic remote write.
    # # @default -- `[]`
     extra_write_relabel_configs:
       - source_labels: [ __name__ ]
         regex: ^envoy_cluster_membership_total$
         target_label: newrelic_metric_type
         replacement: "gauge"
         action: replace
       - source_labels: [ __name__ ]
         regex: ^envoy_server_main_thread_watchdog_miss|envoy_listener_downstream_cx_overflow|envoy_cluster_upstream_rq_completed|envoy_http_downstream_rq_rx_reset|envoy_http_tracing_service_forced|envoy_http_health_check_ok|envoy_http_health_check_cached_response|envoy_cluster_original_dst_host_invalid|envoy_http_downstream_rq_tx_reset|envoy_cluster_external_upstream_rq|envoy_listener_downstream_cx_transport_socket_connect_timeout|envoy_cluster_manager_cds_init_fetch_timeout|envoy_cluster_upstream_cx_destroy_remote_with_active_rq|envoy_cluster_ssl_ocsp_staple_requests|envoy_cluster_upstream_rq_retry_limit_exceeded|envoy_cluster_internal_upstream_rq_xx|envoy_listener_extension_config_missing|envoy_cluster_upstream_rq_pending_failure_eject|envoy_http_downstream_rq_http1_total|envoy_dns_cares_resolve_total|envoy_listener_manager_lds_update_success|envoy_http_downstream_cx_http2_total|envoy_cluster_lb_subsets_fallback_panic|envoy_listener_manager_listener_removed|envoy_cluster_manager_cds_update_failure|envoy_filesystem_flushed_by_timer|envoy_listener_admin_downstream_listener_filter_remote_close|envoy_dns_cares_timeouts|envoy_cluster_upstream_cx_destroy_local_with_active_rq|envoy_dns_cares_reinits|envoy_control_plane_rate_limit_enforced|envoy_listener_admin_downstream_global_cx_overflow|envoy_cluster_upstream_cx_rx_bytes_total|envoy_http_rq_direct_response|envoy_tcmalloc_released_by_timer|envoy_cluster_upstream_rq|envoy_http_health_check_degraded|envoy_cluster_http2_inbound_window_update_frames_flood|envoy_http_downstream_rq_ws_on_non_ws_route|envoy_cluster_internal_upstream_rq|envoy_cluster_update_failure|envoy_http_downstream_cx_max_duration_reached|envoy_cluster_lb_zone_routing_cross_zone|envoy_sds_xds_certificate_update_success|envoy_sds_xds_certificate_init_fetch_timeout|envoy_cluster_upstream_cx_destroy|envoy_http_health_check_failed_cluster_not_found|envoy_server_envoy_notifications|envoy_listener_manager_lds_update_attempt|envoy_cluster_lb_recalculate_zone_structures|envoy_dns_cares_get_addr_failure|envoy_cluster_upstream_cx_destroy_remote|envoy_listener_admin_downstream_listener_filter_error|envoy_cluster_http2_rx_messaging_error|envoy_cluster_upstream_cx_destroy_with_active_rq|envoy_http_downstream_cx_total|envoy_server_envoy_bug_failures|envoy_cluster_upstream_cx_max_requests|envoy_cluster_ssl_ciphers|envoy_listener_downstream_pre_cx_timeout|envoy_listener_admin_downstream_cx_total|envoy_cluster_upstream_rq_pending_overflow|envoy_http_rq_reset_after_downstream_response_started|envoy_sds_xds_trusted_ca_update_success|envoy_listener_manager_listener_modified|envoy_cluster_init_fetch_timeout|envoy_http_downstream_rq_response_before_rq_complete|envoy_listener_http_downstream_rq_completed|envoy_cluster_http2_dropped_headers_with_underscores|envoy_cluster_http2_inbound_empty_frames_flood|envoy_cluster_upstream_rq_retry_success|envoy_sds_xds_trusted_ca_init_fetch_timeout|envoy_cluster_manager_update_out_of_merge_window|envoy_server_wip_protos|envoy_listener_downstream_listener_filter_error|envoy_cluster_upstream_rq_rx_reset|envoy_http_downstream_cx_delayed_close_timeout|envoy_http_health_check_failed|envoy_http_rds_update_success|envoy_cluster_assignment_stale|envoy_http_downstream_rq_completed|envoy_cluster_http2_trailers|envoy_http_rq_total|envoy_sds_xds_certificate_update_attempt|envoy_cluster_client_ssl_socket_factory_ssl_context_update_by_sds|envoy_http_rq_redirect|envoy_http1_dropped_headers_with_underscores|envoy_cluster_manager_cluster_updated_via_merge|envoy_cluster_upstream_cx_connect_fail|envoy_cluster_ssl_connection_error|envoy_cluster_http2_inbound_priority_frames_flood|envoy_http_downstream_rq_total|envoy_main_thread_watchdog_miss|envoy_main_thread_watchdog_mega_miss|envoy_cluster_upstream_rq_retry_backoff_ratelimited|envoy_server_dynamic_unknown_fields|envoy_sds_xds_certificate_key_rotation_failed|envoy_sds_xds_trusted_ca_key_rotation_failed|envoy_cluster_http2_outbound_flood|envoy_envoy_overload_actions_reset_high_memory_stream_count|envoy_http_rs_too_large|envoy_cluster_upstream_cx_total|envoy_http_downstream_rq_rejected_via_ip_detection|envoy_cluster_default_total_match_count|envoy_http_no_cluster|envoy_cluster_http1_metadata_not_supported_error|envoy_cluster_upstream_rq_per_try_timeout|envoy_http_downstream_rq_failed_path_normalization|envoy_server_main_thread_watchdog_mega_miss|envoy_http_downstream_cx_http3_total|envoy_cluster_upstream_cx_close_notify|envoy_filesystem_write_buffered|envoy_listener_downstream_listener_filter_remote_close|envoy_cluster_upstream_rq_maintenance_mode|envoy_cluster_manager_cluster_added|envoy_cluster_upstream_cx_destroy_local|envoy_cluster_lb_local_cluster_not_ok|envoy_http_passthrough_internal_redirect_too_many_redirects|envoy_listener_network_extension_config_missing|envoy_listener_admin_main_thread_downstream_cx_total|envoy_listener_downstream_cx_destroy|envoy_cluster_upstream_flow_control_drained_total|envoy_http_downstream_rq_redirected_with_normalized_path|envoy_server_static_unknown_fields|envoy_cluster_assignment_timeout_received|envoy_cluster_lb_subsets_selected|envoy_cluster_update_attempt|envoy_workers_watchdog_mega_miss|envoy_cluster_upstream_flow_control_resumed_reading_total|envoy_cluster_http2_outbound_control_flood|envoy_http_downstream_cx_protocol_error|envoy_cluster_upstream_internal_redirect_succeeded_total|envoy_runtime_load_success|envoy_cluster_upstream_cx_idle_timeout|envoy_cluster_zone_us_west_1b__upstream_rq_xx|envoy_http_downstream_cx_destroy|envoy_http_downstream_cx_idle_timeout|envoy_cluster_upstream_rq_retry_overflow|envoy_http_downstream_rq_too_many_premature_resets|envoy_cluster_manager_cluster_removed|envoy_cluster_ssl_curves|envoy_http_passthrough_internal_redirect_bad_location|envoy_cluster_ssl_fail_verify_error|envoy_http_passthrough_internal_redirect_no_route|envoy_http_downstream_rq_overload_close|envoy_cluster_ssl_fail_verify_san|envoy_cluster_zone_us_west_1b__upstream_rq_completed|envoy_http_downstream_cx_rx_bytes_total|envoy_cluster_update_rejected|envoy_cluster_http1_response_flood|envoy_listener_manager_lds_update_rejected|envoy_cluster_manager_cds_update_success|envoy_cluster_manager_cds_update_attempt|envoy_server_debug_assertion_failures|envoy_cluster_upstream_rq_total|envoy_http_rds_update_empty|envoy_http_rq_overload_local_reply|envoy_cluster_lb_zone_cluster_too_small|envoy_cluster_update_empty|envoy_cluster_upstream_cx_pool_overflow|envoy_cluster_internal_upstream_rq_completed|envoy_listener_admin_downstream_cx_destroy|envoy_cluster_upstream_internal_redirect_failed_total|envoy_http_health_check_failed_cluster_empty|envoy_cluster_lb_subsets_created|envoy_http_downstream_cx_http1_total|envoy_http_downstream_cx_destroy_remote|envoy_http_no_route|envoy_listener_admin_http_downstream_rq_completed|envoy_listener_downstream_cx_total|envoy_http_rds_update_rejected|envoy_sds_xds_certificate_update_rejected|envoy_http_passthrough_internal_redirect_predicate|envoy_listener_admin_no_filter_chain_match|envoy_filesystem_reopen_failed|envoy_cluster_upstream_rq_cancelled|envoy_http_passthrough_internal_redirect_unsafe_scheme|envoy_overload_envoy_resource_monitors_global_downstream_max_connections_failed_updates|envoy_http_downstream_flow_control_paused_reading_total|envoy_cluster_update_success|envoy_filesystem_write_completed|envoy_cluster_ssl_handshake|envoy_cluster_ssl_session_reused|envoy_runtime_load_error|envoy_http1_requests_rejected_with_underscores_in_headers|envoy_listener_manager_lds_update_failure|envoy_cluster_assignment_use_cached|envoy_listener_manager_listener_in_place_updated|envoy_http_downstream_cx_ssl_total|envoy_listener_admin_downstream_pre_cx_timeout|envoy_cluster_retry_or_shadow_abandoned|envoy_listener_worker_downstream_cx_total|envoy_cluster_upstream_rq_pending_total|envoy_http_downstream_rq_xx|envoy_cluster_http1_dropped_headers_with_underscores|envoy_http_tracing_client_enabled|envoy_http_tracing_not_traceable|envoy_cluster_ssl_ocsp_staple_responses|envoy_runtime_override_dir_not_exists|envoy_cluster_upstream_cx_connect_attempts_exceeded|envoy_http_downstream_cx_drain_close|envoy_cluster_ssl_fail_verify_cert_hash|envoy_listener_downstream_global_cx_overflow|envoy_cluster_upstream_cx_connect_timeout|envoy_cluster_lb_zone_routing_all_directly|envoy_http_rds_update_attempt|envoy_cluster_client_ssl_socket_factory_upstream_context_secrets_not_ready|envoy_sds_xds_certificate_update_failure|envoy_cluster_bind_errors|envoy_http_downstream_rq_http3_total|envoy_listener_manager_listener_stopped|envoy_cluster_http2_header_overflow|envoy_cluster_upstream_cx_none_healthy|envoy_filesystem_write_failed|envoy_http_downstream_rq_header_timeout|envoy_http_downstream_cx_destroy_remote_active_rq|envoy_cluster_http2_tx_reset|envoy_cluster_lb_subsets_fallback|envoy_cluster_upstream_cx_http2_total|envoy_http_downstream_cx_destroy_local|envoy_http_downstream_rq_timeout|envoy_cluster_upstream_rq_retry_backoff_exponential|envoy_http_downstream_rq_non_relative_path|envoy_server_worker_watchdog_mega_miss|envoy_cluster_ssl_sigalgs|envoy_cluster_upstream_cx_connect_with_0_rtt|envoy_runtime_deprecated_feature_use|envoy_workers_watchdog_miss|envoy_cluster_manager_cds_update_rejected|envoy_http_tracing_health_check|envoy_listener_admin_downstream_cx_overload_reject|envoy_sds_xds_trusted_ca_update_failure|envoy_cluster_upstream_cx_http3_total|envoy_cluster_upstream_cx_protocol_error|envoy_cluster_http2_metadata_empty_frames|envoy_http_health_check_failed_cluster_unhealthy|envoy_http_health_check_request_total|envoy_cluster_lb_zone_routing_sampled|envoy_http1_metadata_not_supported_error|envoy_http_downstream_rq_too_large|envoy_http_tracing_random_sampling|envoy_cluster_external_upstream_rq_xx|envoy_http_downstream_flow_control_resumed_reading_total|envoy_runtime_override_dir_exists|envoy_http_rds_update_failure|envoy_http_downstream_cx_max_requests_reached|envoy_http_downstream_cx_upgrades_total|envoy_listener_manager_listener_added|envoy_server_worker_watchdog_miss|envoy_cluster_client_ssl_socket_factory_downstream_context_secrets_not_ready|envoy_cluster_upstream_rq_max_duration_reached|envoy_cluster_external_upstream_rq_completed|envoy_http_downstream_rq_max_duration_reached|envoy_cluster_manager_cluster_modified|envoy_listener_manager_listener_create_failure|envoy_cluster_http1_requests_rejected_with_underscores_in_headers|envoy_cluster_lb_healthy_panic|envoy_cluster_manager_cds_config_reload|envoy_listener_admin_downstream_cx_transport_socket_connect_timeout|envoy_cluster_upstream_cx_overflow|envoy_listener_manager_listener_create_success|envoy_cluster_upstream_rq_0rtt|envoy_http1_response_flood|envoy_http_downstream_rq_idle_timeout|envoy_sds_xds_trusted_ca_update_attempt|envoy_cluster_update_no_rebuild|envoy_http_downstream_cx_destroy_local_active_rq|envoy_http_downstream_rq_http2_total|envoy_http_downstream_cx_destroy_active_rq|envoy_cluster_upstream_rq_retry|envoy_cluster_http2_tx_flush_timeout|envoy_cluster_membership_change|envoy_cluster_ssl_no_certificate|envoy_cluster_manager_cluster_updated|envoy_listener_admin_downstream_cx_overflow|envoy_cluster_zone_us_west_1b__upstream_rq|envoy_cluster_lb_subsets_removed|envoy_cluster_upstream_rq_xx|envoy_cluster_upstream_cx_http1_total|envoy_cluster_upstream_cx_tx_bytes_total|envoy_cluster_upstream_flow_control_backed_up_total|envoy_listener_admin_http_downstream_rq_xx|envoy_listener_downstream_cx_overload_reject|envoy_cluster_lb_zone_no_capacity_left|envoy_cluster_upstream_flow_control_paused_reading_total|envoy_cluster_http2_rx_reset|envoy_http_downstream_cx_overload_disable_keepalive|envoy_server_dropped_stat_flushes|envoy_cluster_http2_requests_rejected_with_underscores_in_headers|envoy_cluster_upstream_cx_max_duration_reached|envoy_http_downstream_cx_tx_bytes_total|envoy_listener_manager_lds_init_fetch_timeout|envoy_cluster_http2_keepalive_timeout|envoy_cluster_http2_headers_cb_no_stream|envoy_cluster_http2_stream_refused_errors|envoy_cluster_ssl_ocsp_staple_omitted|envoy_cluster_ssl_was_key_usage_invalid|envoy_cluster_upstream_rq_per_try_idle_timeout|envoy_dns_cares_not_found|envoy_http_rds_init_fetch_timeout|envoy_listener_no_filter_chain_match|envoy_cluster_manager_update_merge_cancelled|envoy_cluster_ssl_versions|envoy_listener_http_downstream_rq_xx|envoy_cluster_ssl_ocsp_staple_failed|envoy_cluster_upstream_rq_timeout|envoy_sds_xds_trusted_ca_update_rejected|envoy_cluster_upstream_rq_tx_reset|envoy_cluster_http2_goaway_sent|envoy_cluster_upstream_http3_broken|envoy_http_rds_config_reload|envoy_cluster_ssl_fail_verify_no_cert$
         target_label: newrelic_metric_type
         replacement: "counter"
         action: replace
       - source_labels: [ __name__ ]
         regex: ^keda_resource_registered_total|keda_trigger_registered_total$
         target_label: newrelic_metric_type
         replacement: "gauge"
         action: replace
       - source_labels: [ __name__ ]
         regex: ^nginx_ingress_controller_requests|nginx_ingress_controller_check_success|nginx_ingress_controller_success$
         target_label: newrelic_metric_type
         replacement: "counter"
         action: replace

    #   # Enable the extra_write_relabel_configs below for backwards compatibility with legacy POMI labels.
    #   # This helpful when migrating from POMI to ensure that Prometheus metrics will contain both labels (e.g. cluster_name and clusterName).
    #   # For more migration info, please visit the [migration guide](https://docs.newrelic.com/docs/infrastructure/prometheus-integrations/install-configure-prometheus-agent/migration-guide/).
    #   - source_labels: [namespace]
    #     action: replace
    #     target_label: namespaceName
    #   - source_labels: [node]
    #     action: replace
    #     target_label: nodeName
    #   - source_labels: [pod]
    #     action: replace
    #     target_label: podName
    #   - source_labels: [service]
    #     action: replace
    #     target_label: serviceName
    #   - source_labels: [cluster_name]
    #     action: replace
    #     target_label: clusterName
    #   - source_labels: [job]
    #     action: replace
    #     target_label: scrapedTargetKind
    #   - source_labels: [instance]
    #     action: replace
    #     target_label: scrapedTargetInstance

    # -- Set up the proxy used to send metrics to New Relic.
    # @default -- `""`
    # proxy_url:

    # -- Use proxy URL indicated by environment variables (HTTP_PROXY, http_proxy, HTTPS_PROXY, https_proxy, and no_proxy) can be configured via config.extraEnvs
    # @default -- `""`
    # proxy_from_environment:

    # -- # Timeout for requests to the remote write endpoint.
    # @default -- `30s`
    # remote_timeout:

    # -- Fine-tune remote-write behavior: <https://prometheus.io/docs/practices/remote_write/#remote-write-tuning>.
    # queue_config:
      # -- Remote Write shard capacity.
      # @default -- `2500`
      # capacity:
      # -- Maximum number of shards.
      # @default -- `200`
      # max_shards:
      # -- Minimum number of shards.
      # @default -- `1`
      # min_shards:
      # -- Maximum number of samples per send.
      # @default -- `500`
      # max_samples_per_send:
      # -- Maximum time a sample will wait in the buffer.
      # @default -- `5s`
      # batch_send_deadline:
      # -- Initial retry delay. Gets doubled for every retry.
      # @default -- `30ms`
      # min_backoff:
      # -- Maximum retry delay.
      # @default -- `5s`
      # max_backoff:
      # -- Retry upon receiving a 429 status code from the remote-write storage.
      # @default -- `false`
      # retry_on_http_429:
      # -- Maximum sample age before dropping.
      # @default -- `0s`
      # sample_age_limit:

  # -- (object) It includes additional remote-write configuration. Note this configuration is not parsed, so valid
  # [prometheus remote_write configuration](https://prometheus.io/docs/prometheus/latest/configuration/configuration/#remote_write)
  # should be provided.
  extra_remote_write:

  # -- It allows defining scrape jobs for Kubernetes in a simple way.
  # @default -- See `values.yaml`
  kubernetes:
    # NewRelic provides a list of Dashboards, alerts and entities for several Services. The integrations_filter configuration
    # allows to scrape only the targets having this experience out of the box.
    # If integrations_filter is enabled, then the jobs scrape merely the targets having one of the specified labels matching
    # one of the values of app_values.
    # Under the hood, a relabel_configs with 'action=keep' are generated, consider it in case any custom extra_relabel_config is needed.
    integrations_filter:
      # -- enabling the integration filters, merely the targets having one of the specified labels matching
      #    one of the values of app_values are scraped. Each job configuration can override this default.
      enabled: true
      # -- source_labels used to fetch label values in the relabel config added by the integration filters configuration
      source_labels: ["app.kubernetes.io/name", "app.newrelic.io/name", "k8s-app"]
      # -- app_values used to create the regex used in the relabel config added by the integration filters configuration.
      # Note that a single regex will be created from this list, example: '.*(?i)(app1|app2|app3).*'
      app_values: ["redis", "traefik", "calico", "nginx", "coredns", "kube-dns", "etcd", "cockroachdb", "velero", "harbor", "argocd", "gateway-helm", "envoy", "keda", "istio", "ztunnel", "node-exporter"] #gateway-helm=envoy-gateway

    # Kubernetes jobs define [kubernetes_sd_configs](https://prometheus.io/docs/prometheus/latest/configuration/configuration/#kubernetes_sd_config)
    # to discover and scrape Kubernetes objects. Besides, a set of relabel_configs are included in order to include some Kubernetes metadata as
    # Labels. For example, address, metrics_path, URL scheme, prometheus_io_parameters, namespace, pod name, service name and labels are taken
    # to set the corresponding labels.
    # Please note, the relabeling allows configuring the pod/endpoints scrape using the following annotations:
    # - `prometheus.io/scheme`: If the metrics endpoint is secured then you will need to set this to `https`
    # - `prometheus.io/path`: If the metrics path is not `/metrics` override this.
    # - `prometheus.io/port`: If the metrics are exposed on a different port to the service for service endpoints or to
    #   the default 9102 for pods.
    # - `prometheus.io/param_<param-name>`: To include additional parameters in the scrape URL.
    jobs:
    # 'default' scrapes all targets having 'prometheus.io/scrape: true'.
    # Out of the box, since kubernetes.integrations_filter.enabled=true then only targets selected by the integration filters are considered.
    - job_name_prefix: default
      fallback_scrape_protocol: "PrometheusText0.0.4"
      target_discovery:
        pod: true
        endpoints: true
        filter:
          annotations:
            prometheus.io/scrape: true
      # -- integrations_filter configuration for this specific job. It overrides kubernetes.integrations_filter configuration
      # integrations_filter:

      # 'newrelic' scrapes all targets having 'newrelic.io/scrape: true'.
      # This is useful to extend the targets scraped by the 'default' job allowlisting services leveraging `newrelic.io/scrape` annotation
    - job_name_prefix: newrelic
      fallback_scrape_protocol: "PrometheusText0.0.4"
      integrations_filter:
        enabled: false
      target_discovery:
        pod: true
        endpoints: true
        filter:
          annotations:
            newrelic.io/scrape: true

    # -- Set up the job name prefix. The final Prometheus `job` name will be composed of <job_name_prefix> + the target discovery kind. ie: `default-pod`
    # @default -- `""`
    # - job_name_prefix:

      # -- The target discovery field allows customizing how Kubernetes discovery works.
      # target_discovery:

        # -- Whether pods should be discovered.
        # @default -- `false`
        # pod:

        # -- Whether endpoints should be discovered.
        # @default -- `false`
        # endpoints:

        # -- Defines filtering criteria, it is possible to set labels and/or annotations. All filters will apply (defined
        # filters are taken into account as an "AND operation").
        # @default -- `{}`
        # filter:
          # -- Map of annotations that the targets should have. If only the annotation name is defined, the filter only checks if exists.
          # @default -- `{}`
          # annotations:

          # -- Map of labels that the targets should have. If only the label name is defined, the filter only checks if exists.
          # @default -- `{}`
          # labels:

      # -- Set up the proxy used to send metrics to New Relic.
      # @default -- `""`
      # proxy_url:

      # -- Use proxy URL indicated by environment variables (HTTP_PROXY, http_proxy, HTTPS_PROXY, https_proxy, and no_proxy) can be configured via config.extraEnvs
      # @default -- `""`
      # proxy_from_environment:

      # -- Advanced configs of the Kubernetes service discovery `kuberentes_sd_config` options,
      # check [prometheus documentation](https://prometheus.io/docs/prometheus/latest/configuration/configuration/#kubernetes_sd_config) for details.
      # Notice that using `filter` is the recommended way to filter targets to avoid adding load to the API Server.
      # additional_config:
        # kubeconfig_file: ""
        # namespaces: {}
        # selectors: {}
        # attach_metadata: {}


    # -- The HTTP resource path on which to fetch metrics from targets.
    # Use `prometheus.io/path` pod/service annotation to override this or modify it here.
    # @default -- `/metrics`
    # metrics_path:

    # -- Optional HTTP URL parameters.
    # Use `prometheus.io/param_<param-name>` pod/service annotation to include additional parameters in the scrape url or modify it here.
    # @default -- `{}`
    # params:

    # -- Configures the protocol scheme used for requests.
    # Annotate the service/pod with `prometheus.io/scheme=https` if the secured port is used or modify it here.
    # @default -- `http`
    # scheme:

    # -- How frequently to scrape targets from this job.
    # @default -- defined in `common.scrape_interval`
    # scrape_interval:

    # -- Per-scrape timeout when scraping this job.
    # @default -- defined in `common.scrape_timeout`
    # scrape_timeout:

    # -- Configures the scrape request's TLS settings.
    # @default -- `{}`
    # tls_config:
      # -- CA certificate file path to validate API server certificate with.
      # @default -- `""`
      # ca_file:

      # -- Certificate and key files path for client cert authentication to the server.
      # @default -- `""`
      # cert_file:
      # key_file:

      # Disable validation of the server certificate.
      # @default -- `false`
      # insecure_skip_verify:

    # -- Sets the `Authorization` Bearer token header on every scrape request
    # @default -- `{}`
    # authorization:
      # Sets the credentials to the credentials read from the configured file.
      # @default -- `""`
      # credentials_file:

    # -- Sets the `Authorization` header on every scrape request with the configured username and password.
    # @default -- `{}`
    # basic_auth:
      # username:
      # password_file:

    # -- List of relabeling configurations. Used if needed to add any special filter or label manipulation before the scrape takes place.
    # @default -- `[]`
    # extra_relabel_config:

    # -- List of metric relabel configurations. Used it to filter metrics and labels after scrape.
    # @default -- `[]`
    # extra_metric_relabel_config:


  # -- It allows defining scrape jobs for targets with static URLs.
  # @default -- See `values.yaml`.
  static_targets:
    # -- List of static target jobs. By default, it defines a job to get self-metrics. Please note, if you define `static_target.jobs` and would like to keep
    # self-metrics you need to include a job like the one defined by default.
    # @default -- See `values.yaml`.
    jobs:
    - job_name: self-metrics
      skip_sharding: true  # sharding is skipped to obtain self-metrics from all Prometheus servers.
      targets:
        - "localhost:9090"
#      extra_relabel_config:
#        - source_labels: [ __meta_kubernetes_pod_name ]
#          separator: ;
#          target_label: pod
#          replacement: "my-evil-pod"
#          action: replace
      extra_metric_relabel_config:
        - source_labels: [__name__]
          regex: "\
            prometheus_agent_active_series|\
            prometheus_target_interval_length_seconds|\
            prometheus_target_scrape_pool_targets|\
            prometheus_remote_storage_samples_pending|\
            prometheus_remote_storage_samples_in_total|\
            prometheus_remote_storage_samples_retried_total|\
            prometheus_agent_corruptions_total|\
            prometheus_remote_storage_shards|\
            prometheus_sd_kubernetes_events_total|\
            prometheus_agent_checkpoint_creations_failed_total|\
            prometheus_agent_checkpoint_deletions_failed_total|\
            prometheus_remote_storage_samples_dropped_total|\
            prometheus_remote_storage_samples_failed_total|\
            prometheus_sd_kubernetes_http_request_total|\
            prometheus_agent_truncate_duration_seconds_sum|\
            prometheus_build_info|\
            process_resident_memory_bytes|\
            process_virtual_memory_bytes|\
            process_cpu_seconds_total|\
            prometheus_remote_storage_bytes_total"
          action: keep

    # -- The job name assigned to scraped metrics by default.
    # @default -- `""`.
    # - job_name:
      # -- List of target URLs to be scraped by this job.
      # @default -- `[]`.
      # targets:

      # -- Labels assigned to all metrics scraped from the targets.
      # @default -- `{}`.
      # labels:

      # -- The HTTP resource path on which to fetch metrics from targets.
      # @default -- `/metrics`
      # metrics_path:

      # -- Optional HTTP URL parameters.
      # @default -- `{}`
      # params:

      # -- Configures the protocol scheme used for requests.
      # @default -- `http`
      # scheme:

      # -- How frequently to scrape targets from this job.
      # @default -- defined in `common.scrape_interval`
      # scrape_interval:

      # -- Per-scrape timeout when scraping this job.
      # @default -- defined in `common.scrape_timeout`
      # scrape_timeout:

      # -- Set up the proxy used to send metrics to New Relic.
      # @default -- `""`
      # proxy_url:

      # -- Use proxy URL indicated by environment variables (HTTP_PROXY, http_proxy, HTTPS_PROXY, https_proxy, and no_proxy) can be configured via config.extraEnvs
      # @default -- `""`
      # proxy_from_environment:

      # -- Configures the scrape request's TLS settings.
      # @default -- `{}`
      # tls_config:
        # -- CA certificate file path to validate API server certificate with.
        # @default -- `""`
        # ca_file:

        # -- Certificate and key files path for client cert authentication to the server.
        # @default -- `""`
        # cert_file:
        # key_file:

        # Disable validation of the server certificate.
        # @default -- `false`
        # insecure_skip_verify:

      # -- Sets the `Authorization` Bearer token header on every scrape request
      # @default -- `{}`
      # authorization:
        # Sets the credentials to the credentials read from the configured file.
        # @default -- `""`
        # credentials_file:

      # -- Sets the `Authorization` header on every scrape request with the configured username and password.
      # @default -- `{}`
      # basic_auth:
        # username:
        # password_file:

      # -- List of relabeling configurations. Used if needed to add any special filter or label manipulation before the scrape takes place.
      # @default -- `[]`
      # extra_relabel_config:

      # -- List of metric relabel configurations. Used it to filter metrics and labels after scrape.
      # @default -- `[]`
      # extra_metric_relabel_config:


  #  If configuring environment variables from configmaps or secrets, ensure the configmap or secret exists before enabling this
  # extraEnvs:
  #   - name: HTTPS_PROXY
  #     value: "http://example.com" # Direct value
  #   - name: HTTPS_PROXY
  #     valueFrom:
  #       configMapKeyRef:
  #         name: my-configmap
  #         key: proxy
  #   - name: HTTPS_PROXY
  #     valueFrom:
  #       secretKeyRef:
  #         name: my-secret
  #         key: proxy

  # -- It is possible to include extra scrape configuration in [prometheus format](https://prometheus.io/docs/prometheus/latest/configuration/configuration/#scrape_config).
  # Please note, it should be a valid Prometheus configuration which will not be parsed by the chart.
  # WARNING extra_scrape_configs is a raw Prometheus config. Therefore, the metrics collected thanks to it will not have by default the metadata (pod_name, service_name, ...) added by the configurator for the static or kubernetes jobs.
  # This configuration should be used as a workaround whenever kubernetes and static job do not cover a particular use-case.
  # @default -- `[]`
  extra_scrape_configs:
    - job_name: 'istio-envoy-stats'
      metrics_path: /stats/prometheus
      kubernetes_sd_configs:
        - role: pod
      relabel_configs:
        - source_labels: [ __meta_kubernetes_pod_container_port_name ]
          action: keep
          regex: '.*-envoy-prom'
        - separator: ;
          regex: __meta_kubernetes_pod_annotation_prometheus_io_param_(.+)
          replacement: __param_$1
          action: labelmap
        - separator: ;
          regex: __meta_kubernetes_pod_label_(.+)
          replacement: $1
          action: labelmap
        - source_labels: [ __meta_kubernetes_namespace ]
          separator: ;
          target_label: namespace
          replacement: $1
          action: replace
        - source_labels: [ __meta_kubernetes_pod_node_name ]
          separator: ;
          target_label: node
          replacement: $1
          action: replace
        - source_labels: [ __meta_kubernetes_pod_name ]
          separator: ;
          target_label: pod
          replacement: $1
          action: replace