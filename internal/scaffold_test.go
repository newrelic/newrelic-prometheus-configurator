package internal

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

// TestScaffold is defined Just to set up the scaffold and add testing dependencies. TODO: remove when we are ready.
func TestScaffold(t *testing.T) {
	assert.True(t, true)
}
